install! 'cocoapods', :disable_input_output_paths => true

source 'https://github.com/CocoaPods/Specs.git'

platform :osx, '14.0'

target 'ZappaStream' do
  use_frameworks!
  pod 'VLCKit', '~> 3.6.0'
end